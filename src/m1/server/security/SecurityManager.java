package m1.server.security;

import m2.element.Component;
import m2.element.Configuration;

public class SecurityManager extends Component {

	public SecurityManager(String name, Configuration configuration,
			SecProvidedPort port) {
		super(name, configuration, port);
	}

}
