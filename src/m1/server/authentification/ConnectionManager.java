package m1.server.authentification;

import m2.element.Component;
import m2.element.Configuration;

public class ConnectionManager extends Component {

	public ConnectionManager(String name, Configuration configuration,
			AuthProvidedPort port) {
		super(name, configuration, port);
	}

}
